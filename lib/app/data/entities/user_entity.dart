class UserEntity {
  String? accessToken;
  String? displayName;
  String? email;
  String? photoUrl;
  String? id;

  UserEntity(
      {this.accessToken, this.displayName, this.email, this.photoUrl, this.id});
}
